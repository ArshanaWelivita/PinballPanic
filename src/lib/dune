(library
  (name bumper)
  (modules
    bumper)
  (libraries core))

(library
  (name tunnel)
  (modules
    tunnel)
  (libraries core))

(library
  (name grid)
  (modules grid)
  (libraries 
    core
    bumper)
  (preprocess
  (pps ppx_jane)))

